{
  "name": "front",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "front": "npx tsc --project tsconfig.json",
    "browsersync": "npm run front && npx browser-sync start --server 'public' --port 5000 --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.2"
  }
}
